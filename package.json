{
  "name": "publish-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "build": "pnpm run build:site && pnpm run build:epub && pnpm run verify:output",
    "build:site": "node publishing/build-site.js",
    "build:epub": "node publishing/build-epubs.js",
    "verify:output": "node publishing/verify-output.js",
    "test": "node --test"
  },
  "engines": {
    "node": "22.23.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "markdown-it": "14.3.0",
    "yaml": "2.8.1"
  }
}
